# 阶段一：基座克隆与源码解剖报告

> 目标：评估 `Serein-81/financial_rag` 是否适合作为“新人培训多专家 RAG 系统”的改造基座。
> 状态：已完成

## 1. 克隆结果

- 本地路径：`F:\论文方向\大模型+RAG知识库\financial_rag`
- 仓库：`https://github.com/Serein-81/financial_rag.git`
- 分支：`main`
- HEAD：`94b16cd feat(backend): implement policy notification agent and event services`
- 工作树：干净，未修改上游源码
- 运行时：Python 3.11.7，Node.js v22.23.2

## 2. 基座结论

适合作为改造基座，但不是轻量 Demo：

- 后端：FastAPI + Python
- 前端：Vue 3 + TypeScript + Vite
- 编排：LangGraph `StateGraph`
- 检索：pgvector / 混合检索相关模块
- 数据与基础设施：PostgreSQL、Redis、Neo4j、MinIO、Docker
- Agent：自研 ReAct/Plan/Reflect 执行层 + LangGraph 宏观编排层
- 质量闭环：CRAG 检索评分/查询改写、Faithfulness 检查、Reflection、重试和人工审核

## 3. 请求处理主链路

```text
用户请求
  -> receptionist
  -> intent
  -> rag_retrieval
  -> retrieval_grader
  -> query_rewriter（低质量时循环回检索）
  -> 单专家或多专家并行
  -> aggregator
  -> faithfulness_checker
  -> reflection / regenerate_aggregator
  -> final_answer
```

核心实现：

- `financial_rag/rag_backend/app/langgraph/graph.py`
  - `MultiAgentWorkflowBuilder` 负责构建状态图。
  - 默认支持 checkpointer、reflection、CRAG、faithfulness check。
  - 当前专家节点是 `finance_specialist`、`tax_specialist`、`legal_specialist`、`report_specialist`。
- `financial_rag/rag_backend/app/langgraph/state.py`
  - `AgentState` 使用 Pydantic `BaseModel`。
  - `rag_context`、`specialist_results`、`messages` 等字段使用 `operator.add` reducer 合并并行结果。
  - 已有检索质量、忠实度、重生成、人工审核等状态字段。
- `financial_rag/rag_backend/app/langgraph/conditional.py`
  - 负责意图路由、专家路由、并行分发和质量节点后的条件跳转。

## 4. 领域迁移的关键位置

### Agent 与路由

- `financial_rag/rag_backend/app/multi_agent_system/config/agent_capabilities.yaml`
  - 当前绑定 finance / tax / legal / general 能力、关键词和路由规则。
- `financial_rag/rag_backend/app/prompts/agents/intent_router/system.md`
  - 当前定义财税法务意图及专家映射。
- `financial_rag/rag_backend/app/prompts/agents/orchestrator/system.md`
  - 当前以财务、税务、法务任务拆解和汇总。
- `financial_rag/rag_backend/app/prompts/agents/finance/system.md`
- `financial_rag/rag_backend/app/prompts/agents/tax/system.md`
- `financial_rag/rag_backend/app/prompts/agents/legal/system.md`
- `financial_rag/rag_backend/app/prompts/agents/output/system.md`

后续新人培训映射：

| 当前专家 | 新人培训专家 |
|---|---|
| finance | 业务与产品 SOP 专家 |
| tax | HR 与行政制度专家 |
| legal | IT 与工程规范专家 |
| report / orchestrator | 带教导师、评测与仲裁专家 |

### 领域分块

- `financial_rag/rag_backend/app/chunkers/domain_detector.py`
  - `DomainDetector.detect(filename, parsed_doc=None, kb_category=None) -> str`
  - 当前领域白名单为 `finance/tax/legal/general`。
- `financial_rag/rag_backend/app/chunkers/README.md`
  - 已有条款级、表格原子化、AST 双层节点、Auto-Merging、Markdown 和纯文本等策略。

后续新人培训建议：

- 制度类：按章节、条款、审批条件切分；保留生效时间和适用对象。
- IT 手册：按任务步骤和代码块切分；保留前置条件、命令和验证方式。
- 业务 SOP：按流程节点、角色、输入输出和异常分支切分。
- 通用培训材料：复用 General / Markdown 策略。

## 5. 阶段一发现的风险

1. 当前工作流和提示词仍是财税法务领域，不能直接作为新人培训系统使用。
2. `SpecialistType`、路由配置、领域检测器和专家节点名称存在多处耦合，不能只改提示词。
3. 项目依赖 PostgreSQL、Redis、Neo4j、MinIO 等服务，不能假设 `pip install` 后即可启动。
4. `main.py` 的生命周期会初始化较多基础设施，建议阶段二先确认最小可运行链路，再决定保留哪些企业级模块。
5. 阶段二修改路由时必须同步检查 `graph.py`、`state.py`、`conditional.py`、Agent 注册表、YAML 配置、Prompt 和测试，避免出现枚举/节点/路由不一致。

## 6. 阶段一结论

- 基座通过：保留 FastAPI、Vue3、LangGraph、CRAG、Faithfulness 和领域分块体系。
- 暂不改源码：阶段一只完成克隆、源码定位和迁移边界确认。
- 下一阶段：先设计新人培训领域模型和专家契约，再做最小纵向切片：`意图路由 -> 一个专家 -> RAG -> 可验证回答`。

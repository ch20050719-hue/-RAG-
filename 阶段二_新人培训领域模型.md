# 阶段二：新人培训领域模型与专家契约

## 1. 目标

将财税法务知识库改造成企业新人培训助手，同时保留旧领域标识的兼容能力，避免一次改造破坏原有数据和测试。

## 2. 专家团

| 专家 | 标识 | 负责内容 | 不负责 |
|---|---|---|---|
| HR 与行政制度专家 | `hr_policy` | 员工手册、考勤休假、福利报销、转正与绩效制度 | 技术配置、业务决策 |
| IT 与工程规范专家 | `it_engineering` | 环境搭建、账号权限、Git/代码规范、研发流程、故障排查 | HR 制度解释、业务规则 |
| 业务与产品 SOP 专家 | `business_product` | 产品知识、业务术语、岗位流程、客户服务与异常处理 | 人事审批、底层技术排障 |
| 带教导师/仲裁专家 | `mentor` | 拆解问题、合并多专家结论、给出学习路径、练习题、引用和不确定性检查 | 编造制度或替代知识库证据 |

`mentor` 是编排层角色，不是文档分块领域；知识文档领域使用前三个标识和 `general`。

## 3. 路由规则

- 单一问题：只调用匹配的领域专家。
- 跨域问题：并行调用多个领域专家，再由 `mentor` 合并。
- 学习计划、练习题、知识点总结：由 `mentor` 处理，并引用检索证据。
- 无法匹配或证据不足：明确说明缺口，必要时转人工审核，不猜测公司制度。

## 4. 分块策略

首个可运行切片复用 `GeneralChunker` 的 Auto-Merging 双粒度策略，同时保留领域标签：

- `hr_policy`：优先按标题/段落保留制度上下文；后续增加条款和适用对象元数据。
- `it_engineering`：优先保留步骤、命令和验证结果；后续增加前置条件元数据。
- `business_product`：优先保留流程节点及角色边界；后续增加输入/输出/异常分支元数据。

## 5. 本阶段已完成的最小改造

- 新增统一领域目录：`financial_rag/rag_backend/app/chunkers/domains.py`
- `DomainDetector` 支持新人培训文件名和显式分类。
- `DomainChunkerFactory` 将三个新人培训领域路由到通用 Auto-Merging 分块器，同时保留领域标签。
- `GeneralChunker` 支持构造时注入领域标签。
- LLM 服务改为按需导入，领域检测纯规则路径不再在模块导入时初始化数据库/LLM 依赖。

## 6. 下一步

下一步再同步修改 `SpecialistType`、条件路由、Agent 注册表和新人培训 Prompt；先增加路由单测，再替换专家节点，避免只改提示词导致状态图节点不一致。

# 智能家居改造过程文档

## 1. 改造结论

本次改造已将项目业务范围收敛为智能家居。RAG 检索、Agent 编排、多轮会话、流式输出、权限与审计等通用能力保留；设备查询、设备控制、安全规则、MQTT 和 ESP32 原型已形成独立模块。旧财税链路不再作为当前产品入口、配置、知识数据或文档内容维护。

## 2. 已完成内容

### 2.1 领域切换

- 家居知识种子覆盖设备说明、环境数据、舒适度、场景和安全规则；
- 意图分析、Graph 查询分类、工具路由和 Agent 能力配置均面向智能家居；
- Home Specialist 接入现有 Agent 编排和多轮会话流程；
- 前端品牌、导航、设置和设备页统一为智能家居表达。

### 2.2 设备与安全

- 增加设备模型、默认设备注册表和设备服务；
- 固定工具支持设备列表、单设备状态、环境读取、灯/风扇控制和预置场景；
- 控制请求经过注册、类型、在线、动作白名单、命令状态、TTL 和幂等校验；
- 控制结果包含 accepted、message、blocked_reason 和 ack 时间，避免把“已发送”误报为“已执行”。

### 2.3 MQTT 与硬件

- 提供模拟适配器和 MQTT 适配器，开发模式默认使用模拟适配器；
- MQTT 控制使用 QoS 1、结构化 JSON、request_id、过期时间和 ack 回执；
- 增加 ESP32 固件，支持低压 LED、5V 风扇、温湿度/光照/人体数据和 availability；
- 固件不接受任意 GPIO 指令，原型禁止接入 220V 市电。

## 3. 当前请求链路

```text
自然语言或前端 API
  → 会话上下文
  → 家居意图识别
  → RAG 检索规则/设备说明
  → Home Agent 选择固定工具
  → 安全校验
  → 模拟或 MQTT 执行
  → 状态/ack 回执
  → 多轮会话结果
```

## 4. 关键代码入口

| 位置 | 用途 |
|---|---|
| `rag_backend/app/home_automation/device_models.py` | 设备、传感器、命令和场景模型 |
| `rag_backend/app/home_automation/device_service.py` | 查询、控制和场景执行 |
| `rag_backend/app/home_automation/safety_rules.py` | 安全检查与阻断原因 |
| `rag_backend/app/home_automation/device_tools.py` | Agent 固定设备工具 |
| `rag_backend/app/home_automation/mqtt_adapter.py` | MQTT 发布、ack 和状态处理 |
| `rag_backend/app/api/v1/endpoints/home_devices.py` | `/api/v1/home` API |
| `rag_frontend/src/views/HomeDevicesView.vue` | 控制台设备页面 |
| `esp32/esp32_home_node/esp32_home_node.ino` | ESP32 桌面原型固件 |

## 5. 验证记录

| 检查项 | 结果 |
|---|---|
| 后端家居与安全单元测试 | 64 passed，1 条 pytest 配置警告 |
| `python -m compileall -q app` | 通过 |
| 前端 `npm run build` | 通过；仅有 Browserslist 和 chunk 体积提示 |
| `git diff --check` | 通过 |
| 完整后端启动 | 当前虚拟环境缺少 `minio`，需补齐依赖后复测 |
| ESP32 实机联调 | 固件和协议已准备，Broker、固件版本和接线版本待实机记录 |

## 6. 后续联调步骤

1. 安装后端完整依赖，确认 FastAPI 应用可启动；
2. 使用 Mosquitto 或其他 MQTT Broker，记录地址、认证、TLS 和 ACL 配置；
3. 烧录 ESP32 固件，记录板卡、库版本、固件提交和接线；
4. 先验证 availability/telemetry，再验证单设备控制和 ack；
5. 最后验证睡眠、离家、观影场景以及断线、超时、重复 request_id 拦截。

## 7. 参考资料

- [Paho MQTT Python Client](https://eclipse.dev/paho/files/paho.mqtt.python/html/client.html)
- [OASIS MQTT 3.1.1](https://docs.oasis-open.org/mqtt/mqtt/v3.1.1/mqtt-v3.1.1.html)
- [ESP-IDF MQTT](https://docs.espressif.com/projects/esp-idf/en/stable/esp32/api-reference/protocols/mqtt.html)
